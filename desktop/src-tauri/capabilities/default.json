{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认能力集：包含 Tauri 默认权限 + 窗口拖拽能力（用于 data-tauri-drag-region）",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
