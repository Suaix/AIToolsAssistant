# UI/UX 设计：迁移测试隔离与 GUI 陈旧弹窗清理

> **任务编号**：FIX-001
> **创建日期**：2026-05-11
> **状态**：跳过（Skipped）

---

## 跳过说明

本任务为**纯技术 bug 修复 + dead code 清理**，零 UI 变更、零用户交互路径变化。DESIGN 三项 checklist 均不适用：

| 检查项 | 跳过原因 |
|---|---|
| 页面流程完整 | 无页面变更 |
| 组件规范引用 | 不涉及 UI 组件 |
| 交互状态覆盖 | GUI 侧仅增加"弹窗不出现"的负行为，无新交互路径 |

GUI 侧唯一的"可感知变化"是：**启动时若 `.last-migration.json` 指向的 `.bak` 已不存在，不再弹出误导性弹窗**——这符合 L2 设计原则「诚实先于友好」（与其展示不准确信息，不如不展示）。

相关"设计"职责（数据流 / 调用时序 / 边界条件）归并至 `03-technical.md` 的第 2 章架构。

manifest 的 `design_to_technical` 门禁 3 项 skipped 均已标注理由。
